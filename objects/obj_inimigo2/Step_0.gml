// Inherit the parent event


maquina_de_estado();

var _tam_sprite = bbox_right-bbox_left;

tam_barra = 2 * (_tam_sprite / 2);

x_barra = x + _tam_sprite/2 - tam_barra/2;

if (alarm[1] <= 0){
		
hp_secundario = lerp(hp_secundario, vida, 0.1)
}



retorna_mola();
contador_efeito_branco()

if (layer_sequence_exists("sq_alerta", seq) && layer_sequence_is_finished(seq)) {
    layer_sequence_destroy(seq);
    seq = noone;
}

